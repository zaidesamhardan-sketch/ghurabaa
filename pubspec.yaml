name: ghurabaa
description: تطبيق غُرَبَاء - الرفيق الروحي اليومي
publish_to: 'none'
version: 2.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.3
  flutter_local_notifications: ^22.0.1
  audioplayers: ^6.0.0
  shared_preferences: ^2.2.3
  vibration: ^3.2.0
  adhan: ^2.0.0+1
  hijri: 3.0.1
  fl_chart: ^1.2.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/audio/
  # تم تعطيل الخطوط مؤقتاً لتجنب خطأ عدم العثور على الملفات أثناء الـ Build